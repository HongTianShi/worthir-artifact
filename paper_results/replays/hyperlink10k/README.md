# Hyperlink10k Released Replay

`public/` contains the action interface and legal state.
`organizer_private/` contains evaluator-only-at-action-time outcomes released
for this completed 10,000-query scale/route stress. The directory name is
kept for scorer compatibility and must not be interpreted as policy input.

This surface reuses all Structured-v2 query IDs inside a larger population;
it is a dependent retrospective stress, not an independent task-family
confirmation.
