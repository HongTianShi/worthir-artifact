# Quickstart Participant View

This directory contains only information available to the example routing policy.

- `legal_state.csv` is the small routing-time state table.
- `example_actions.json` is a legal action vector selecting one registered route per query.

No relevance judgments, utilities, oracle actions, or outcomes for unselected routes belong here. The example is synthetic and demonstrates the interface rather than a trained policy's performance.
