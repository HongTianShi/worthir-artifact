# Scoring smoke test

This six-query synthetic task checks strict action validation, cumulative costs,
utility, and exact within-route-set regret. Run it as part of:

```powershell
.\worthir.cmd doctor
```

For a complete qrels-to-report example, use `.\worthir.cmd demo` instead.
