# Hyperlink10k 已发布回放

`public/` 包含动作接口和合法状态。`organizer_private/` 包含这个已完成的 10,000
查询规模/路线压力测试中，在动作时只对评测者可见、现已发布的结果。保留该目录名是为了
兼容评分器，不能将其中内容视为策略输入。

该接口在更大样本中重复使用了全部 Structured-v2 查询 ID，因此属于存在依赖的
回顾性压力测试，而不是独立任务族的验证。
