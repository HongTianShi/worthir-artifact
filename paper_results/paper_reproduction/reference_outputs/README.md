# Archived Reference Figures

`figure1.pdf`, `figure2.pdf`, and `figure3.pdf` are tracked reference outputs for the archived `paper_reproduction` workflow. They allow a user to compare regenerated figures with the release snapshot.

They are not the source of scientific values and are not guaranteed to match the final accepted IP&M artwork. Regenerate into a fresh output directory; do not overwrite these files during ordinary validation.
