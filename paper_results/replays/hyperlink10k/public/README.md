# WorthIR Hyperlink10k 参与者接口

该软件包只公开购买时合法状态、5 视图路线注册表、累计操作成本及 5 组冻结的主数据划分。
对于一次运行，需要为公共测试集的每个 `query_uid` 提交且只提交一个已注册视图，
然后运行 `validate_submission.py`。

任务对数据集提供的上下文标题候选进行重排。超链接视图购买额外的语料库衍生文本证据，
用于为这些候选评分；它们并非开放语料候选检索。公共 ID/问题仍可关联到公共源数据集，
因此该契约保证的是时间顺序和字段隔离，而不是密码学标签保密。
